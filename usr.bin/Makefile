# Makefile for usr.bin

.include <bsd.own.mk>

# NetBSD imports
SUBDIR= indent m4 stat tic sed mkdep uniq

# Non-NetBSD imports
SUBDIR+= ministat

# Minix commands
SUBDIR+= top mkimage

.include <bsd.subdir.mk>
